import { Controller } from '../../Core/Controller.js'

export class AuthController extends Controller {
  constructor(req, res) {
    super(req, res)
  }

  login() {
    // Login logic here
  }
  logout() {}
}

export enum OrderStatus {
    'pending' = 'pending',
    'paid' = 'paid',
    'processing' = 'processing',
    'shipped' = 'shipped',
    'delivered' = 'delivered',
    'refunded' = 'refunded',
    'cancelled' = 'cancelled',
}
